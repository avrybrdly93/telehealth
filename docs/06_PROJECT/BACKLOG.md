---
title: Backlog
status: Active
authority: Project
owner: Product
dependencies:
  - PROJECT_STATUS.md
  - ../01_PRODUCT/SERVICE_REQUIREMENTS.md
review_cycle: Weekly
---

# Backlog

## How to use (agents)
- Take the topmost item with status `Ready` whose Deps are all `Done`. Order = priority.
- Sizing: **S** ≈ one session, **M** ≈ two sessions (checkpoint mid-way), **L** = must be split before starting (grooming task).
- Statuses: Ready · In Progress · Blocked · Needs Human Review · Done.
- New items use ../../templates/FEATURE_TEMPLATE.md fields; bugs are inserted above features of the same milestone using ../../templates/BUG_TEMPLATE.md.
- References column lists the docs to read in Phase 1 of the session.

## Bugs

| ID | Item | Size | Deps | Status | References | Acceptance criteria |
|---|---|---|---|---|---|---|
| BUG-001 | GitHub Pages deploy workflow fails: `withastro/action@v3` defaults to Node 20, Astro 7.1.6 requires Node >=22.12.0 | S | — | Done | templates/BUG_TEMPLATE.md, .github/workflows/deploy.yml | `Deploy to GitHub Pages` workflow run succeeds (build + deploy jobs both green) on a push to `main` |
| BUG-002 | Playwright/Lighthouse hit a 404: astro.config.mjs's `base: '/telehealth'` (added for GitHub Pages) never propagated to playwright.config.ts's baseURL or lighthouserc.cjs's collect.url | S | — | Done | templates/BUG_TEMPLATE.md, playwright.config.ts, lighthouserc.cjs | `pnpm exec playwright test` and `lhci autorun` both pass locally against the built site |
| BUG-003 | `font-display: swap`'s post-paint font swap caused a real CLS budget breach (0.15 vs 0.1) on `/services/medication-management`, first caught because lighthouserc.cjs only ever collected `/` — never measured on other routes before | S | — | Done | templates/BUG_TEMPLATE.md, lighthouserc.cjs, src/layouts/BaseLayout.astro | `lhci autorun` passes CLS budget on every URL in lighthouserc.cjs's `collect.url` |
| BUG-004 | Auto-merged pushes to `main` (`.github/workflows/auto-merge-claude.yml`, default `GITHUB_TOKEN`) don't trigger `ci.yml`/`deploy.yml`'s `push` triggers — GitHub suppresses cascaded workflow runs from actions-authored pushes, so `main`'s deployed site silently lags behind every session's auto-merge until someone notices and runs `workflow_dispatch` by hand (confirmed live: the BL-013 auto-merge at 2026-07-31 10:30 UTC triggered no `ci.yml`/`deploy.yml` run at all) | S | — | Done | templates/BUG_TEMPLATE.md, .github/workflows/auto-merge-claude.yml, .github/workflows/ci.yml, .github/workflows/deploy.yml, PROJECT_STATUS.md Weekly Review 2026-07-30 | `ci.yml` and `deploy.yml` both run and pass automatically off the very next `claude/*` auto-merge (not a manual `workflow_dispatch`) — verified live this session: `deploy.yml` run 30637925559 and `ci.yml` run 30637925630 both fired via `workflow_run` and both completed with conclusion `success` |
| BUG-005 | **S1 — every internal link site-wide drops the `/telehealth` GitHub Pages base, 404ing on production.** All in-app hrefs (`SiteHeader` nav/logo/Book button, `SiteFooter`, every page's cross-links and CTA buttons — 13 source files, see Reproduction) are hardcoded root-relative (`href="/pricing"`, `href="/"`, etc.) with no base prefix. Astro does not auto-rewrite plain string hrefs for a non-root `base` (confirmed: built `dist/pricing/index.html` literally has `href="/pricing"`); the browser resolves that against the origin, not the `/telehealth/` base, so on the live site (`avrybrdly93.github.io/telehealth/`) clicking *any* nav link, footer link, or in-page CTA/cross-link navigates to `avrybrdly93.github.io/pricing` (etc.) — a 404. Same root cause also silently breaks `SiteHeader`'s `aria-current="page"` in production (`currentPath` from `Astro.url.pathname` *does* include the base, e.g. `/telehealth/pricing`, so it never equals the bare `href="/pricing"` used for comparison — confirmed via built HTML: no `aria-current` attribute renders on any nav link). Existing E2E coverage did not catch this: `tests/e2e/nav-audit.spec.ts` (UX-003) clicks through nav links but asserts `toHaveURL(/\/pricing\/?(?:[?#]|$)/)`, an unanchored regex that matches the base-dropped URL just as well as the correct one — verified live with a throwaway Playwright script against `pnpm preview` (`href="/pricing"` → click → lands on `http://localhost:4321/pricing`, base dropped) that the bug reproduces while the existing test suite stays green. Found by BL-030 session (2026-08-02) while adding canonical URLs; not fixed there per scope discipline — filed here instead. | S | — | Done | templates/BUG_TEMPLATE.md, src/lib/routes.ts, src/components/SiteHeader/SiteHeader.astro, src/components/SiteFooter/SiteFooter.tsx, src/pages/*.astro (11 pages), playwright.config.ts, tests/e2e/nav-audit.spec.ts | Every internal `href` site-wide resolves through a single helper (e.g. `withBase(path)` using `import.meta.env.BASE_URL`, same pattern `BaseLayout.astro` already uses for font/OG-image URLs) instead of a hardcoded string; a real Playwright assertion (anchored full-URL or `page.url()` equality, not the current unanchored regex) proves clicking each nav link from each route lands on the correct `/telehealth/...` URL; `SiteHeader`'s `aria-current="page"` renders correctly in a real (non-base-root) build, not just in SiteHeader.test.tsx's isolated prop-matching unit test — **all verified, see CHANGELOG session 21**: `src/lib/routes.ts#withBase()` added; `SiteHeader.astro` logo/nav/mobile-menu/Book hrefs and `isCurrent()` fixed (base + trailing-slash-tolerant compare); `SiteFooter.tsx` nav/legal hrefs fixed; all 11 page files' hardcoded hrefs (`/book`, `/pricing`, `/services/[id]`, `/providers/[id]`, `/faq`, `/`, `/services`, `/contact`) fixed, including `index.astro`'s `<Hero>` `primaryCtaHref`/`secondaryCtaHref` props (found during the fix, not in the original repro list, still same root cause); `nav-audit.spec.ts` now asserts an anchored full-URL regex built from `playwright.config.ts`'s exported `BASE_URL` (proved it actually catches the regression: temporarily stubbed `withBase()` back to identity, all 17 desktop cases failed as expected, then restored). Root cause: Astro does not rewrite plain string `href`s for a non-root `base` — confirmed unchanged/documented in `withBase()`'s doc comment for the next session. |
| BUG-006 | **S3 — provider photo placeholder `<img src>` has the same missing-`/telehealth`-base root cause as BUG-005, 404s in production.** `PROVIDER_PHOTO_PLACEHOLDER = '/images/provider-photo-placeholder.svg'` is hardcoded root-relative in 4 files (`src/pages/providers/index.astro`, `src/pages/providers/[slug].astro`, `src/pages/about.astro`, `src/pages/index.astro`); confirmed in built `dist/providers/index.html`: `src="/images/provider-photo-placeholder.svg"`, missing the base the same way BUG-005's hrefs were. Lower severity than BUG-005: it's a decorative placeholder image with `alt=""` (D-005, DECISION_LOG.md) — broken image icon is a visual/professionalism issue, not a broken flow or an a11y regression (screen readers already skip it via empty alt). Found while fixing BUG-005 (session 21, 2026-08-02); not fixed there — `withBase()` is for hrefs/navigation per its doc comment, and expanding it (or adding a same-shaped `img`-src helper) mid-session would be scope creep on a claimed item, so filed here instead. | S | — | Done | src/pages/providers/index.astro, src/pages/providers/\[slug\].astro, src/pages/about.astro, src/pages/index.astro, src/lib/routes.ts (BUG-005 precedent) | **Done, session 22 (2026-08-02)**: all 4 `PROVIDER_PHOTO_PLACEHOLDER` references now resolve through `withBase()` (reused directly rather than a separate asset-path helper — its doc comment broadened to cover `src` as well as `href`, same underlying behavior); confirmed via a real `pnpm build`: `dist/index.html`/`dist/about/index.html`/`dist/providers/index.html`/`dist/providers/dr-md/index.html` all have `src="/telehealth/images/provider-photo-placeholder.svg"`, no bare `src="/images/..."` remains. New `tests/e2e/provider-photo.spec.ts` asserts the built `<img src>` on all 4 pages against `playwright.config.ts`'s `BASE_URL`, both viewports (8 cases) — full suite 148/150 passed, 2 correctly skipped (same baseline as every prior session). |
| BUG-007 | **S3 — `ci.yml` has no `workflow_dispatch` trigger, so a failed or stuck CI run cannot be re-triggered without pushing another commit.** `.github/workflows/ci.yml`'s `on:` block is `push` / `pull_request` / `workflow_run` only. `deploy.yml` already carries `workflow_dispatch`, and that is the only reason session 39 could obtain a green deploy at `49e7071` after the push-triggered run at that same SHA hung — the identical escape hatch does not exist for CI. Hit live in session 39: `ci.yml` run `31117857469` at `49e7071` concluded `failure` because its `e2e-axe-lighthouse` job was `cancelled` after sitting 15m01s with `runner_id: 0` and zero steps (never assigned a runner); the API's `rerun_failed_jobs` was accepted (HTTP 201, new attempt queued 22:18:05Z) but produced **no jobs at all** over the following ~25 minutes, so there was no way to get CI signal at that commit short of pushing a new one. Distinct from the GitHub outage session 38 documented: the missing trigger is a standing gap in this repo's workflow that the outage merely exposed, and it will bite again on any transient runner-scheduling failure. | S | — | Done (2026-08-07 session 40) | templates/BUG_TEMPLATE.md, .github/workflows/ci.yml, .github/workflows/deploy.yml (already has the trigger — copy the one-line pattern), CHANGELOG.md session 39 | `ci.yml`'s `on:` block gains `workflow_dispatch:` alongside its existing `push`/`pull_request`/`workflow_run` triggers, with none of those three altered; proven by actually dispatching it — a `workflow_dispatch` run of `ci.yml` against `main` is accepted by the API and completes with **both** `lint-typecheck-build` and `e2e-axe-lighthouse` green. YAML that merely parses is not sufficient evidence. |
| BUG-008 | **S2 — `deploy.yml` has no `concurrency:` block, so two pushes to `main` close together race and the second Pages deployment is rejected outright.** Deploy run **113** (`49f1d9c`, the current `main` HEAD) failed with `HttpError: Deployment request failed for 49f1d9c… due to in progress deployment. Please cancel a5f1631… first or wait for it to complete.` The `build` job was **green** — `withastro/action@v3` succeeded in 21s — so this is not a build failure; only `actions/deploy-pages@v4` failed, in 1 second, and `smoke` was skipped. Runs 112 (`a5f1631`, 14:34:06Z) and 113 (`49f1d9c`, 14:34:52Z) overlapped: 112's Pages deployment was still in flight (it finished 14:39:31Z) when 113 tried to create its own at 14:35:30Z. GitHub's own Pages starter workflow carries `concurrency: {group: pages, cancel-in-progress: false}` for exactly this; this repo's copy never had it. Consequence: **the newer commit's build is silently discarded** and `main`'s HEAD is not what is served — here that cost only two doc files, but the same race drops real content. | S | — | Done | templates/BUG_TEMPLATE.md, .github/workflows/deploy.yml, DEPLOYMENT_AND_OPERATIONS_PLAN.md §Pipeline | A `Deploy to GitHub Pages` run at the fixed HEAD concludes `success` with `build`, `deploy` and `smoke` all green — YAML that parses is not evidence (BUG-007 precedent), a live green run is. Plus a unit test asserting the top-level `concurrency` block is present with `group: pages` and `cancel-in-progress: false`, so the guard cannot be deleted unnoticed. **Met (session 78):** run **114** at `7faa7bf` concluded `success` with **all three jobs green** — `build` 26s (`withastro/action@v3` 20s), `deploy` 8s (`actions/deploy-pages@v4` **succeeded in 5s**, against 1s to failure in run 113), `smoke` 4s with all three checks passing (homepage 200, sitemap.xml non-empty, `/book` Step 1 renders). Unit test green at 160/160 across 24 files. **Caveat recorded rather than glossed:** run 114 was the only deployment in flight, so it proves the workflow still deploys, not that the serialisation queues a second run — that needs two overlapping runs, and manufacturing them is not worth the churn. |

| BUG-009 | **S2 — every route scrolls horizontally at 375px.** `document.documentElement.scrollWidth` measured **653px in a 375px viewport** on `/`, and the page really did pan: `window.scrollTo(500, 0)` left `window.scrollX` at **278**. Not a measurement artefact of an off-canvas element — there is no `overflow-x: hidden` anywhere, so the document itself was over-wide. Three independent causes, all confirmed by walking every element whose right edge passed the viewport: (1) `SiteHeader`'s `.bar` is a `display:flex` row whose `.logo` is a flex item at the default `min-width:auto`, so it refused to shrink and pushed the actions cluster to x=652; (2) unfilled `NEEDS_HUMAN_*` constants are single unbreakable words (`NEEDS_HUMAN_PROVIDER_MD_APPROACH` and friends) and dictated the intrinsic width of every card containing one — a provider card measured 583px; (3) `/pricing`'s table has a deliberately `white-space: nowrap` price cell holding `NEEDS_HUMAN_EVALUATION_PRICE`, which no amount of wrapping can fix, widening that page to 526px. **Found 2026-08-31 (session 108) by BL-040's new overflow spec.** Nothing caught it before: axe does not flag horizontal overflow, Lighthouse does not fail on it, and all 274 e2e cases passed against it. Causes (2) and (3) shrink once real content lands, but cause (1) does not, and the demo the owner wants to show is on placeholders today | S | — | Done (2026-08-31 session 108) | src/styles/global.css, src/components/SiteHeader/SiteHeader.module.css, src/components/PricingTable/PricingTable.tsx, tests/e2e/keyboard-and-layout.spec.ts, docs/02_UX/ACCESSIBILITY.md | `document.documentElement.scrollWidth <= clientWidth + 1` on every route in `SITE_ROUTES` plus `/404`, at both the 375px and 1280px projects, asserted in the suite so it cannot regress silently — **met, 42 cases green**. Fixed with: `overflow-wrap: anywhere` on `body` (`anywhere`, not `break-word` — only `anywhere` feeds back into intrinsic sizing, which is what the flex/grid layout pass consults); `flex-wrap: wrap` on `.bar`, `min-width: 0` on `.logo`, `flex-shrink: 0` on `.actions`; and a labelled, keyboard-focusable `overflow-x: auto` scroll container around the pricing table, so its overflow stays inside the table (`role="region"` + `tabIndex={0}`, or a mouse-only scroll box is itself an axe `scrollable-region-focusable` violation). 160/160 Vitest and the full axe sweep still green after the change |
| BUG-010 | **S3 — mobile menu links are 30px tall against ACCESSIBILITY.md's documented 44×44px floor.** `.mobileNavLink` had no box of its own, so the tappable area was exactly the height of its text. The 24px `gap` between rows made the menu *look* generously spaced, which is likely why it survived review — but spacing between targets is not target size, and only the latter is what a thumb has to hit. Measured 30px for all five links (`Services`, `Providers`, `Pricing`, `About`, `FAQ`). **Found 2026-08-31 (session 108) by BL-040's touch-target spec**, which asserts the repo's own written standard rather than a WCAG minimum — ACCESSIBILITY.md line 27 says "Touch targets ≥ 44×44px with ≥ 8px spacing", and this was the project failing its own rule | S | — | Done (2026-08-31 session 108) | docs/02_UX/ACCESSIBILITY.md, src/components/SiteHeader/SiteHeader.module.css, tests/e2e/keyboard-and-layout.spec.ts | Every link in the open mobile menu, plus the menu toggle itself, measures ≥44px in both dimensions at 375px, asserted in the suite — **met**. Fixed by giving `.mobileNavLink` `display: flex; align-items: center; min-height: 44px` (growing the target, not the gap). The toggle already met it via its own `min-width`/`min-height` and is now covered by the same test |

## Milestone M1 — Foundation

| ID | Item | Size | Deps | Status | References | Acceptance criteria |
|---|---|---|---|---|---|---|
| BL-001 | Scaffold Astro+TS project, repo structure per ARCHITECTURE.md, CI skeleton (lint/typecheck/build), CLAUDE.md at root | M | — | Done | ARCHITECTURE, TECH_STACK, CODING_STANDARDS, CLAUDE_DEVELOPMENT_PROTOCOL | `pnpm build` green in CI; repo dirs match ARCHITECTURE.md; CLAUDE.md ≤60 lines with 3 absolute rules |
| BL-002 | Design tokens as CSS custom properties + global styles + font self-hosting | S | BL-001 | Done | DESIGN_TOKENS, DESIGN_SYSTEM, PERFORMANCE_BUDGET | tokens.css matches DESIGN_TOKENS.md exactly; fonts woff2 subsetted ≤120KB; sample page renders both families |
| BL-003 | Content collections + zod schemas (services, providers, conditions, faq, legal) + practice.ts constants with named placeholders | M | BL-001 | Done | ARCHITECTURE §Content boundary, CODING_STANDARDS §Content | invalid frontmatter fails build; placeholder constants flagged NEEDS_HUMAN |
| BL-004 | Core components batch 1: Button, TextInput, TextArea, Checkbox, Card (+tests, all states) | M | BL-002 | Done | COMPONENT_LIBRARY, ACCESSIBILITY, ERROR_STATES E-010 | each component: 5 states, keyboard test, axe-clean in test harness |
| BL-005 | SiteHeader, SiteFooter, CrisisResources, SkipLink + base layout | M | BL-004 | Done | COMPONENT_LIBRARY, INFORMATION_ARCHITECTURE §Navigation, UX-020 | crisis block canonical copy verbatim; mobile menu focus-trapped; axe clean |
| BL-006 | Playwright + axe + LHCI wired into CI with budgets | S | BL-001 | Done | TESTING_AND_VALIDATION_PLAN, PERFORMANCE_BUDGET | CI runs e2e/axe/LHCI on preview; a deliberate violation fails the pipeline |
| BL-007 | Reduce SiteHeader JS payload under the 15KB content-page budget | S | BL-006 | Done | PERFORMANCE_BUDGET, TECH_STACK §Framework, DECISION_LOG D-004, COMPONENT_LIBRARY#SiteHeader | LHCI `resource-summary:script:size` passes at `error` severity on `/` (flip lighthouserc.cjs back from `warn`); SiteHeader.test.tsx + tests/e2e/mobile-menu.spec.ts behavior (focus trap, Esc, aria-expanded) still pass |

## Milestone M2 — Content Pages

| ID | Item | Size | Deps | Status | References | Acceptance criteria |
|---|---|---|---|---|---|---|
| BL-010 | Homepage per spec | M | BL-005, BL-003 | Done | PAGE_SPECIFICATIONS §/, FR-010, COPY_GUIDELINES | FR-010 fold test at 375px passes; GLOBAL-01/02 assertions pass |
| BL-011 | Services index + 2 service pages (content files + template) | M | BL-010 | Done | PAGE_SPECIFICATIONS §/services, TELEHEALTH_SPECIFICATION | prices from practice.ts; durations stated; NFR-007 demo: page = content file only |
| BL-012 | Providers index + 2 bio pages | S | BL-010 | Needs Human Review (2026-07-31 session 11) | PAGE_SPECIFICATIONS §/providers, FR-011, IMAGE_GUIDELINES | credential/license fields from constants; photo alt pattern correct; Needs Human Review before publish — pages built and shipped to `main`; blocked on real provider names/credentials/license numbers/prices (practice.ts), approach statements, education/bio copy, and professional photos (all Tier 3, see PROJECT_STATUS.md "Blocked / Needs Human Input") before this can go live with real content |
| BL-013 | Pricing page | S | BL-011 | Done | PAGE_SPECIFICATIONS §/pricing, FR-013, UX-003 | reachable ≤2 interactions from every page (nav audit test) |
| BL-014 | About + Your First Visit pages | S | BL-010 | Done | PAGE_SPECIFICATIONS, FR-012, FR-015 | copy passes readability CI (see BL-017 — no automated readability CI exists yet; manually estimated Flesch-Kincaid grade ~6.6–8.2, avg sentence length 11.5–13.8 words, within the ≤8th-grade/≤20-word guideline) |
| BL-015 | FAQ page (≥12 Q&As) with accordion + #emergencies anchor | S | BL-010 | Needs Human Review (2026-07-31 session 15) | FR-016, COMPONENT_LIBRARY §FAQAccordion, Flow 4 | 13 Q&As shipped across all 5 required groups (`#getting-started`/`#appointments-policies`/`#costs-superbills`/`#medication-questions`/`#emergencies`, jump nav + required `#emergencies` anchor present); FAQPage JSON-LD schema deferred to BL-031 (its own Deps already list BL-015, i.e. structured data was always scoped there, not here — see CHANGELOG session 15); cancellation-policy and payment-methods answers pull `PLACEHOLDER_CANCELLATION_POLICY`/`PLACEHOLDER_PAYMENT_METHODS` from practice.ts (Needs Human Review before publish, same as /pricing) |
| BL-016 | Legal pages shell + accessibility statement draft; 404 page | S | BL-010 | Done | FR-040, FR-041, E-040 | legal copy marked Blocked pending human text; 404 has crisis strip |
| BL-017 | Automated readability CI script over patient-facing copy (UX-002) | S | — | Done | TECH_STACK.md §Testing tools, TESTING_AND_VALIDATION_PLAN.md §Content validation, COPY_GUIDELINES.md §Style Rules | `scripts/check-readability.ts` runs in CI (`continue-on-error: true`, warns→blocks per TESTING_AND_VALIDATION_PLAN.md and D-008) over `src/content/{services,providers,conditions,faq}` (legal exempt per COPY_GUIDELINES.md) and fails below the 8th-grade threshold; content sourcing BL-011/BL-015 retroactively verified and fixed to pass (see D-008) — BL-010/BL-012/BL-014's prose lives in `.astro` templates, out of this check's scope per D-008, not "retroactively verified" by this item |
| BL-018 | Flip readability CI (BL-017) from `continue-on-error` to blocking | S | BL-032 | Done (2026-08-03 session 25) | DECISION_LOG.md D-008, BACKLOG.md BL-032, COPY_GUIDELINES.md Hard Rule 2 | `conditions/{depression,anxiety,adhd}.md` pass `pnpm run check:readability` (either revised content once BL-032 is drafted, or a Clinical-Team-approved change to Hard Rule 2's mandatory disclaimer sentence) and `ci.yml`'s `continue-on-error: true` is removed from the readability step in the same change — **confirmed**: `check:readability` still passes all 3 files (adhd 7.9, anxiety 7.9, depression 7.7, unchanged since BL-032 drafted them) and `continue-on-error: true` is removed from `ci.yml`'s readability step; full local verification green (lint/typecheck/test 97/97/format/build 20 pages/`check:readability` 16 passed 0 failed 2 skipped/e2e 172 passed 2 skipped/`lhci autorun` 20/20 URLs pass) |

## Milestone M3 — Booking & Contact

| ID | Item | Size | Deps | Status | References | Acceptance criteria |
|---|---|---|---|---|---|---|
| BL-020 | Booking flow steps 1–3 (island) with state preservation + StepIndicator | L→split at grooming | BL-005 | Split (2026-08-03 session 27) → BL-035, BL-036, BL-037 | USER_FLOWS Flow 1, FR-020/021/022, UX-010/011, E-011 | Row kept for history, not implemented directly — too large for one session per this row's own sizing (BACKLOG.md's "How to use"). Split into three session-sized children below; their combined acceptance criteria supersede this row's "BOOK-02/03/04/05 e2e pass" (fully covered once BL-037 ships). See CHANGELOG.md session 27 for the split rationale and DECISION_LOG.md's conflict-hierarchy precedent (D-005/006/010/011) this split follows for component/page decisions deferred to the implementing session. |
| BL-035 | Booking flow scaffold: `/book` route + island shell + state-persistence architecture (URL params/sessionStorage, UX-011) + StepIndicator component + CrisisResources strip wiring + Step 1 (service selection) | M | BL-005 | Done (2026-08-04 session 28) | USER_FLOWS Flow 1, FR-020, FR-022 (early disclosure), FR-024, UX-010/011, COMPONENT_LIBRARY §StepIndicator, §CrisisResources, PAGE_SPECIFICATIONS §/book, ARCHITECTURE §Extensibility (the `selection` state shape BL-021's `buildBookingUrl(selection)` will consume — keep it stable) | `/book` renders Step 1 with a "Step 1 of 4" StepIndicator (aria-live announced on change) and the persistent crisis strip + phone alternative (FR-024, compact variant per COMPONENT_LIBRARY.md); selecting "First appointment (new patient)" vs "Follow-up (existing patients)" (FR-020) persists to URL params/sessionStorage, never cookies (UX-011); Step 1's eligibility summary (CA · 18+ · not for emergencies) shown per FR-022's "early disclosure" note; E-050 no-JS fallback (phone booking instructions) present; StepIndicator gets its own COMPONENT_LIBRARY.md states/a11y notes plus Vitest/RTL/axe coverage — same precedent as Hero/PricingTable/Alert/Breadcrumbs (D-005/D-006/D-010/D-011) — **all confirmed, see CHANGELOG.md session 28**: `lib/booking-state.ts` (12 tests), `StepIndicator` (6 tests incl. a regression test for a real bug found and fixed this session — a literal `"undefined"` CSS class on non-current dots), `BookingFlow` island (7 tests) all real and passing; `lhci autorun` run live against `/book/` and `/` with a new `assertMatrix` giving `/book` its own PERFORMANCE_BUDGET.md "islands" column (70KB JS/100KB image/300KB total vs content pages' 15KB/350KB/500KB) — both passed with no assertion failures; full `pnpm exec playwright test` (262 passed, 2 correctly skipped, same baseline) after fixing two real regressions this session's `chrome="minimal"` decision caused (GLOBAL-02's footer-only crisis-block check, UX-003's pricing-reachable-from-every-page check — both now have documented, deliberate `/book` exceptions, not silent skips). **Not verified**: cross-browser (Safari/Firefox/iOS Safari) — this environment only has Chromium available; only Chromium-based Playwright runs were possible. |
| BL-036 | Booking flow Step 2: provider preference | S | BL-035 | Done (2026-08-04 session 29) | USER_FLOWS Flow 1, FR-021, COMPONENT_LIBRARY §Card (provider variant) | Cards for Dr. [MD] and [PMHNP] plus an equal-weight "No preference — earliest available" default option (FR-021); skippable, and skipping never penalizes wait-time messaging; selection persists into the state mechanism BL-035 established (adds a provider param, pre-fillable from `/book?provider=slug` per Flow 3); browser back from Step 2 to Step 1 preserves Step 1's selection (partial UX-011 — full back-chain verified once BL-037 adds Step 3); crisis strip still present (FR-024) — **all confirmed, see CHANGELOG.md session 29**: `BookingFlow` renders Step 2 via `getCollection('providers')`-sourced `Card` `selectable` cards + a third "No preference" option (deliberately not pre-checked — see COMPONENT_LIBRARY.md), new Step 1 "Continue" (pushState) and Step 2 "Back" (native history) controls, a `popstate` listener as the single source of truth for `currentStep` so the hardware back button and the in-page Back button behave identically; 9 new tests (131/131 total) cover provider selection, the explicit-vs-undecided "No preference" distinction, both back paths, and step-view analytics; full suite verified (typecheck/lint/build/test/e2e/lhci, see CHANGELOG). **Not yet exercised**: an actual Continue-to-Step-3 control (doesn't exist until BL-037) and the full BOOK-02/03 back-chain (needs Step 3 to exist). |
| BL-037 | Booking flow Step 3: eligibility acknowledgments + validation | S | BL-036 | Done (2026-08-04 session 30) | USER_FLOWS Flow 1, FR-022, E-011, UX-011 | **All confirmed, see CHANGELOG.md session 30**: (1) Three explicit `Checkbox` acknowledgments (in CA at time of visit / 18+ / not an emergency) — done, each independent local component state. (2) Continue stays disabled until all three are checked, inline text explains each unmet requirement, never a modal (E-011) — done, `Checkbox`'s `error` prop (already E-011-designated in COMPONENT_LIBRARY.md) renders it live, cleared per-box as each is checked. (3) "not in CA" case links the FAQ answer about California-only care — done, `/faq#getting-started`, the real group-anchor `conditions/[slug].astro` already established (no per-item FAQ anchor exists to link instead). (4) BOOK-04 (E-011) passes — new `tests/e2e/booking-flow.spec.ts`, both viewports. (5) BOOK-03 (browser back Step 3→2→1, selections intact) passes — same spec, both the in-page Back button (Vitest) and hardware back (Playwright) paths covered. (6) BOOK-02 (complete Steps 1–3 via "No preference", arriving at Step 4's entry point) passes — satisfied by Step 3's Continue becoming enabled once all three boxes are checked; Step 4 itself (the vendor-handoff screen, `buildBookingUrl`) is explicitly BL-021's deliverable, not built here — BOOK-01 (the real handoff walkthrough) is BL-021's own acceptance criterion, not this one's. (7) BOOK-05 (crisis strip + phone alternative on Steps 1–3) passes across the full chain — same spec. Closes out original BL-020's acceptance criteria in full. Also: Step 2 gained its own "Continue" button (FR-021, never gated on a selection) so the Step 2→3 hop exists at all; `Checkbox`'s `error` prop widened `string`→`ReactNode` to carry the FAQ link. 143/143 Vitest (12 new), 272/272 e2e (10 new, 2 correctly skipped), `lhci autorun` clean against `/` and `/book/` (zero assertion failures, `/book` JS ~66KB, still under the 70KB budget). |
| BL-021 | Vendor handoff step 4 + buildBookingUrl + mock-vendor e2e | S | BL-037 | Done (2026-08-04 session 31) | FR-023, ARCHITECTURE §Extensibility, DATA_BOUNDARIES B2 | **All confirmed, see CHANGELOG.md session 31**: `buildBookingUrl(selection)` (`lib/vendor-booking.ts`) is the single vendor-swap function ARCHITECTURE §Extensibility requires — serializes `service`/`provider` onto `practice.ts`'s new `PLACEHOLDER_VENDOR_BOOKING_URL` (a fictional `.example`-TLD endpoint; no real vendor chosen yet). `BookingFlow`'s `currentStep` widens to include 4; Step 3's "Continue" now navigates there instead of dead-ending; Step 4 summarizes the selection and its "Continue to secure scheduling" is a real `<a href>` (not a JS redirect) pointing at the built URL, firing `booking_handoff` on click. An unset provider normalizes to the existing `'none'` sentinel so the URL/event never carry an empty `provider_slug` — DATA_BOUNDARIES.md Boundary 2 holds (only service/provider ever leave the client). BOOK-01 passes: new Playwright coverage completes the real Step 1-4 flow with an actual provider selection, mocks the vendor request via `page.route` (this repo's first network-interception test), asserts the handoff URL carries `service`+`provider`, and asserts no other request across the whole flow leaks those values (§Enforcement). 156/156 Vitest (+13: 5 for `buildBookingUrl`, 8 for Step 4 rendering/wiring/analytics/axe), 274/274 e2e (+2, 2 correctly skipped, same baseline), `lhci autorun` clean against `/` and `/book/` (zero assertion failures, `/book` JS ~66.5KB, still under the 70KB budget). **Not verified**: cross-browser (Safari/Firefox/iOS Safari) — same Chromium-only environment limitation as every prior session. |
| BL-022 | Contact page + serverless function + email delivery + failure states | M | BL-005 | In Progress (2026-08-01 session 18) — Next step: D-009 (hosting platform + email vendor, Tier 3, Proposed) needs human resolution before the actual `/api/contact` function can be built; once resolved, stand up the function against `ContactForm.client.ts`'s existing `fetch('/api/contact', {method:'POST', ...})` call (no client-side rework expected), wire real rate limiting server-side, then flip this item to Done. Everything client-side is already shipped: `/contact` page, `ContactForm`/`Alert` components, client validation, honeypot (client-side only — server-side rate limit still gated on the function existing), success/E-030-failure states — all tested and passing on the live GitHub Pages deploy today. | Flow 2, FR-030/031, E-030, SECURITY_AND_COMPLIANCE §controls | CONTACT-01/02 pass; rate limit + honeypot verified; no persistence |
| BL-023 | Analytics wrapper + events per ANALYTICS_PLAN + consent-gating seam | S | BL-010 | Done | ANALYTICS_PLAN, DATA_BOUNDARIES B4, PRIVACY_MODEL | event payloads match schema; no user-input data in any event (test) — `src/lib/analytics.ts`'s `trackEvent()` runtime-enforces the full event/property allowlist (drops anything unlisted even past a forced cast) and strips query strings from route-shaped properties; `pageview`/`cta_book_click`/`crisis_resource_click` wired globally via `BaseLayout.astro`'s bootstrap, `contact_submit_success`/`contact_submit_error`/`error_view` wired into `ContactForm.client.ts`. `booking_*` events are defined in the schema but unwired — `/book` (BL-020/BL-021) doesn't exist yet, next session to wire when built. No analytics vendor configured (DEMO/PROTOTYPE, no real credentials) — default transport is an honest no-op per `setAnalyticsTransport()`'s doc comment, same gap-documented pattern as D-009/`/api/contact`. |

## Milestone M4 — SEO & Launch

| ID | Item | Size | Deps | Status | References | Acceptance criteria |
|---|---|---|---|---|---|---|
| BL-030 | Metadata system, sitemap, robots, canonicals, OG images | S | BL-010 | Done | FR-050, NFR-005, SEO_STRATEGY | GLOBAL-01 passes; sitemap validates — hand-rolled `src/pages/sitemap.xml.ts` (prerendered API route over the new `src/lib/routes.ts#SITE_ROUTES`, shared with `tests/e2e/routes.ts`) generates `sitemap.xml`, no new dependency (`@astrojs/sitemap` was tried first, then removed — adding a runtime dependency is Tier 3 per DECISION_FRAMEWORK.md, not appropriate mid-session without human approval); `public/robots.txt` added; `BaseLayout.astro` emits `<link rel="canonical">` + OG/Twitter tags per page from existing `title`/`description` props; one self-hosted default OG image (`public/images/og-default.jpg`, 1200×630, 45KB). IMAGE_GUIDELINES.md's per-page-dynamic OG image (title text rendered by build) not implemented — judged outside this item's stated acceptance criteria, not a silent gap; a single static default image is used for every page instead. Found BUG-005 (S1) while verifying canonical URLs — filed separately, not fixed here. |
| BL-031 | Structured data (MedicalBusiness, Physician, FAQPage) | S | BL-030, BL-012, BL-015 | Done (2026-08-02 session 23) | FR-051, SEO_STRATEGY §Schema | `src/lib/structuredData.ts` builds all three schema.org shapes (unit-tested, 4 new cases) and each is wired in and confirmed present/valid in a real `pnpm build`: `MedicalBusiness` site-wide via `BaseLayout.astro` (`areaServed: California`, deliberately no `address` field per LOCAL_SEARCH_STRATEGY.md's "no fake address markup"); `Physician` on both provider bio pages (`jobTitle` read from `credential`/practice.ts, not hardcoded, so it stays accurate per-role); `FAQPage` on `/faq` (all 13 Q&As, answers run through `readability.ts#stripMarkdownSyntax` for plain-text `acceptedAnswer.text`). **Not verified this session**: the actual Google Rich Results Test (external tool, needs a live production URL) — that requires this branch's auto-merge + deploy to complete first; next session or a human should run it against the deployed `/`, `/providers/dr-md` (or `/providers/np-pmhnp`), and `/faq` URLs and confirm all three types pass before considering the acceptance criteria fully met. |
| BL-032 | 3 condition pages (depression, anxiety, adhd) drafts | M | BL-011, BL-030 | Needs Human Review (2026-08-03 session 24) | PAGE_SPECIFICATIONS §/conditions, CONTENT_STRATEGY, R-010 | educational disclaimer present (verbatim, all 3); Needs Human Review (provider) before publish — **built this session**: new `Breadcrumbs` component (D-011); `src/pages/conditions/[slug].astro` implements the full `/conditions/[slug]` spec (H1, overview, how-care-helps, breadcrumbs, link to matching service, related-FAQ links, Book CTA, inline crisis strip on the depression page per CONTENT_STRATEGY.md "esp. depression page"); provider bios' "Conditions treated" list now links through to these pages. Content expanded from stub/disclaimer-only to full draft copy per CONTENT_STRATEGY.md's Condition Page Standard (what it feels like, how care helps by category — no drug names, one cited NIMH stat each) and passes `pnpm run check:readability` (grade <=8) for all 3 files — this also unblocks BL-018, whose acceptance criteria these same 3 files now satisfy. Routes registered in `SITE_ROUTES` (sitemap + e2e coverage) and `lighthouserc.cjs` (LHCI budget coverage); full suite verified green (see CHANGELOG session 24). **Not done**: actual clinical/provider review of the drafted content (Tier 3, CONTENT_STRATEGY.md's hard gate) — same "built and tested, content unreviewed" status as BL-012/BL-015. |
| BL-033 | Security headers + smoke tests + uptime monitoring | S | BL-006 | Done (2026-08-25 session 96, Phase-1 scope — see D-012 Resolution) — **smoke-test sub-item Done 2026-08-05 session 32**: `deploy.yml`'s smoke job carried a deferred `/book Step 1 renders` check, commented out in session 26 because `/book` did not exist; BL-021 (session 31) shipped `/book` Steps 1-4, clearing that blocker, and session 32 implemented the check (asserts 200 + the `booking-step-1-heading` id, not copy). This sub-item was NOT gated on D-012 — D-012 gates the header-delivery mechanism and the uptime-monitor vendor only, which remain the reason this item is still In Progress. The `contact function healthcheck` TODO stays deferred (needs `/api/contact`, BL-022/D-009). **Closed 2026-08-25 (session 96)**: D-012 resolved as option 3 — owner direction, accept the header gap as documented residual risk for the pre-launch prototype. This item therefore closes at what it actually delivered: the CSP and `Referrer-Policy` meta tags, `tests/e2e/security-headers.spec.ts` (40 assertions), and the deploy smoke job. **Its original acceptance criteria were not rewritten to fit that** — they move verbatim to BL-038, which is launch-blocking, together with the four header-only controls GitHub Pages cannot deliver and the uptime monitor. See R-013. | DEPLOYMENT_AND_OPERATIONS_PLAN, SECURITY_AND_COMPLIANCE §controls | Phase-1 scope: CSP + `Referrer-Policy` meta tags ship, asserted on every route, and the deploy smoke job is green — all met. Full header scan + monitor alerting move to BL-038, unmet and unclaimed |
| BL-038 | **Launch blocker.** Deliver the four header-only security controls + CSP `frame-ancestors`, and stand up uptime monitoring | S | BL-033, D-009 (host choice) | Blocked (launch-gated; needs a header-delivery mechanism, which D-009's hosting choice may supply) | D-012 (Context §1-3 and Resolution), R-013, SECURITY_AND_COMPLIANCE §Website Security Controls, DEPLOYMENT_AND_OPERATIONS_PLAN §Monitoring & Alerts | **Carried verbatim from BL-033, unmodified:** header scan passes in smoke; monitor alerting verified. Concretely: `X-Content-Type-Options`, `X-Frame-Options`, `Permissions-Policy` and `Strict-Transport-Security` are served as real HTTP response headers on every route, CSP moves from `<meta>` to a header and regains `frame-ancestors`, and an external monitor alerts a human on `/` and `/book`. **Do not close this item by narrowing it** — D-012 deferred it on the explicit basis that the site has no auth, cookies, PHI or persistence pre-launch, and that basis expires the day a real booking flow or contact backend ships. Two open questions belong here, not to a session's judgement: which delivery mechanism (D-012 Resolution §1 — settled implicitly if D-009 picks a host with `_headers` support) and which monitor vendor (Tier 3, D-012 Resolution §2). Cheap win to check first: whether `github.io` is HSTS-preloaded, unverified because the sandbox blocks `hstspreload.org` |
| BL-034 | Launch checklist execution (MVP acceptance criteria) | M | all above | Blocked (deps) | MVP_SCOPE §acceptance | every checkbox evidenced in DECISION_LOG entry |
| BL-039 | **Owner decision.** Dispose of `claude/nice-gates-sxxo8l`, which has carried D-014's and D-015's approved practice constants unmerged for 15 days | S | — | Blocked (owner decision — Tier 3 content, cannot be settled by a session) | DECISION_LOG.md D-014 and D-015, GitHub issues #3 and #4, PROJECT_STATUS.md "Blocked / Needs Human Input" | **Filed 2026-08-31 (session 107) from a diagnosis, not a suspicion.** Issues #3 and #4 (`github-actions[bot]`, both `2026-08-16T20:21:10Z`, byte-identical bodies, one auto-merge failure reported twice) say the branch "could not be merged into main automatically" and direct a human to "fix conflicts in the web editor". Sessions 103-106 recorded the issues but never established what the conflict *is*. It is now established, and it is not what the issue text implies. **(1) The lineage is intact.** `git merge-base origin/main origin/claude/nice-gates-sxxo8l` → `9609ec4`; `main` is 69 ahead, the branch 7 ahead across 16 files. This must be checked on a **complete** clone — the default clone here is shallow (`.git/shallow` present, `git rev-parse --is-shallow-repository` → `true`) and reports `fatal: refusing to merge unrelated histories`, which is exactly the false severed-lineage finding session 103 made and session 104 retracted. `git fetch --unshallow` first, every time. **(2) The conflict is two files, both pure session bookkeeping**: `docs/06_PROJECT/CHANGELOG.md` and `docs/06_PROJECT/PROJECT_STATUS.md`. Everything substantive auto-merges clean — `src/lib/practice.ts`, `src/lib/structuredData.ts`, `src/lib/structuredData.test.ts`, `src/pages/providers/[slug].astro`, `provider-detail.module.css`, `DECISION_LOG.md`, `BACKLOG.md`, and the six spec docs D-014 amended. Verified with `git merge-tree --write-tree --name-only origin/main origin/claude/nice-gates-sxxo8l`, which writes nothing. So the fifteen-day blockage is a changelog collision, not a content dispute. **(3) What merging would publish, and why a session must not do it unasked.** The branch fills `practice.ts`'s `NEEDS_HUMAN_*` constants with real values: provider names Ryan Nelson (MD) and Michael Elhard (PMHNP), phone (909) 888-5555, a $200 flat self-pay rate for both visit types, and the practice name "Nelhardson Psychiatric Care"; it also removes CA license numbers site-wide, including the `identifier` in `buildPhysicianSchema`. D-014 and D-015 record these as Tier 3 **Approved (practice owner, conversational session)** — but that approval is attested only on the branch under review, and a push to `main` fires `deploy.yml` straight to the live GitHub Pages site. Publishing real provider identities and prices on an approval a session cannot verify is the owner's call, which is the whole point of Absolute Rule 2. | Owner decides one of: (a) merge the branch, taking the two bookkeeping files from `main` and keeping every substantive change — a session may then execute it *on explicit instruction*; (b) keep the site on placeholders and close the branch; or (c) re-affirm D-014/D-015 outside the branch so a session can proceed without self-attestation. Whichever is chosen, issues #3 and #4 both close — they are one event. Do **not** close this row by merging on a session's own judgement |
| BL-040 | **E2E coverage expansion.** Extend the Playwright suite past the per-item assertions it grew from, and run it against a dev server on the 3000-3010 port range | S | BL-006 | In Progress (2026-08-31 session 108) | TESTING_AND_VALIDATION_PLAN.md §E2E, playwright.config.ts, tests/e2e/ | **Owner-directed this session** (scheduled-routine instruction: "write a comprehensive Playwright test suite for both UIs, run it, iterate until they pass"), so the "manufacture no claim with zero `Ready` rows" rule does not apply — this is an instruction, not an invented task. The existing 274-case suite is entirely per-backlog-item: every spec file was written to close one row's acceptance criteria (GLOBAL-01/02, BOOK-01…05, UX-003, BUG-006, the axe and CSP sweeps). Nothing covers the shipped surfaces that never had a row of their own. Scope, all **behavioural** and all **additive** (no existing spec rewritten, no assertion relaxed): `/contact`'s `ContactForm` client behaviour end-to-end in a real browser (required-field validation, email-shape validation, first-error focus, honeypot silently faking success without a network call, the E-030 failure state preserving typed text, `aria-busy` during submit) — unit-tested in `ContactForm.client.test.ts` under jsdom but never once driven in a browser; `/faq`'s native `<details>` accordion and its group anchors; the `SkipLink`, which `ACCESSIBILITY.md` requires and no test exercises; **site-wide internal-link integrity** (every internal `href` on every route actually resolves, the class of bug BUG-005 was); `sitemap.xml` agreeing with `SITE_ROUTES` and with what the server serves; canonical/OG/Twitter/JSON-LD per route; console errors and failed requests per route; and 375px horizontal-overflow. `playwright.config.ts`'s port moves 4321 → **3000** (`E2E_PORT` override) and gains an `E2E_SERVER=dev` mode so the same suite runs against `astro dev` as well as `astro preview` | Every new spec passes on both existing viewport projects against **both** server modes (`astro preview` and `astro dev`) on port 3000, with no existing assertion weakened, skipped or deleted, and the pre-existing 274 cases still green; the suite catches something the old one did not — proven by an actual induced-regression check, not by inspection (BUG-005 precedent: a green suite is not evidence of coverage) |
| BL-041 | `robots.txt` ships to `/telehealth/robots.txt`, where no crawler will ever read it | S | BL-030 | Ready | SEO_STRATEGY.md §Technical Foundation, public/robots.txt, tests/e2e/seo-metadata.spec.ts | **Found 2026-08-31 (session 108) while writing BL-040's sitemap coverage, not fixed there** (scope discipline — BL-040 is test work). `public/robots.txt` is served correctly, at `https://avrybrdly93.github.io/telehealth/robots.txt`, and its `Sitemap:` line is right. But the robots.txt protocol is origin-scoped: a crawler fetches `https://avrybrdly93.github.io/robots.txt` and nothing else, and on a GitHub Pages **project** site that path belongs to the account's user site, which this repo does not control. So the file has no effect in production today. Verified in the served build, not inferred. Consequences are small right now (the policy is `Allow: /`, i.e. the default, and the sitemap is also discoverable via Search Console) but the `Sitemap:` declaration is silently doing nothing. Options, in rough order of cost: (a) accept and document it, dropping the file, since `Allow: /` is already the default; (b) submit the sitemap directly in Search Console and record that as the discovery path; (c) publish a `robots.txt` from the account's user-site repo — outside this repo, needs the owner. Not a launch blocker either way | A decision is recorded in DECISION_LOG.md and the repo state matches it: either the file is removed with the reason noted in SEO_STRATEGY.md, or the sitemap's real discovery path is documented, or an owner-side user-site `robots.txt` is confirmed live. `tests/e2e/seo-metadata.spec.ts`'s robots test is updated to match whichever is chosen — it currently asserts what the build ships and carries a pointer to this row |

## Icebox (do not start; Phase 2 candidates)
Patient portal shell · insurance info pages · blog CMS · Spanish localization · additional providers template · online payment of superbill copies.
