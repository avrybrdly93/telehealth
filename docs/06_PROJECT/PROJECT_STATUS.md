---
title: Project Status
status: Active
authority: Project
owner: Engineering
dependencies:
  - BACKLOG.md
  - CHANGELOG.md
review_cycle: Every session
---

# Project Status

> Updated by the agent at the END of every session (EXECUTION_LOOP.md Phase 5). This file is the first thing every session reads. Keep it under 60 lines: current truth only — history lives in CHANGELOG.md.

## Snapshot

- **Phase**: M1/M2 done. M3 — `/book` Steps 1-4 Done (BL-035/036/037/021); BL-022 (`/api/contact` backend) is the only M3 gap, gated on D-009. M4 — BL-030/031/018 Done; BL-032 Needs Human Review; **BL-033 Done 2026-08-25 at its Phase-1 scope** (D-012 resolved, option 3); **BL-038 filed as the launch blocker** carrying BL-033's original criteria verbatim. BUG-001…008 Done. D-012 is **Approved and must not be re-opened**; D-009 is **owner-deferred (2026-08-25) and must not be re-raised**.
- **Last session**: 2026-09-17 (session 159). **Verification only — nothing claimable, and no claim manufactured.** `Ready` tallied from `BACKLOG.md`'s own Status column with the documented recipe (fifth column = `awk -F'|'` **field 6**), first pass, no deviation: **46 status-bearing rows, 0 whose status field begins `Ready`**. **This is the 68th true-zero session.** Split **35 Done / 1 Split (BL-020) / 4 Needs Human Review / 4 Blocked / 1 In Progress / 1 em-dash (BUG-005)** — a **thirteenth** independent tally agreeing, so the **35-vs-36 question stays closed**. The one In Progress row is BL-022, gated on D-009, owner-deferred. Gate green, run first-hand: `--frozen-lockfile` clean in **5.1s**, eslint clean, prettier clean, typecheck **0/0/35 across 88 files**, **160/160** unit across 24 files in 13.37s, readability **16/0/2**, build Complete at 21 pages cross-checked against 21 `.html` files counted directly in a freshly-removed `dist/`; **e2e and `lhci` deliberately not re-run** on a `src/`-unchanged tree. **`deploy.yml` run 199 on `2a2cd9c` — the current `main` head — concluded `success`**, read at the source, `event` **`workflow_run`**, exactly as session 158 predicted (artefact, not a signal). **The routine's red-Pages first-priority clause is stale for the 104th session**, and its lockfile hypothesis was refuted first-hand again. Both gap checks unchanged, neither re-escalated: **#3/#4** still `OPEN`, `updated_at` == `created_at`, no third number. On a complete clone (**shallow again — twelfth consecutive session — unshallowed before any count**, 390 commits) `ls-remote` returns three refs, `kind-newton-1mzu11` **0 ahead** (contained) and `nice-gates-sxxo8l` **7 ahead** at `93865ad5`, merge base `9609ec4`; counts taken against `origin/main`, never the stale local `main`. **`merge-tree` deliberately not re-run.** **The prompt copy changed more than any entry here has recorded: it names `Computing-Platform`, works it FIRST in a FIXED three-repo order, and drops `launcher`** — so the state-file-date rule that kept sending runs to `paper-trader` first did not apply. Computing-Platform was reached first and real work landed and pushed there. **`paper-trader` was probed first, inside itself, before its backlog was read: the 403 returned and ZERO commits were stranded.** The ninth session to get the order right. **Probe a repo's write path before doing its work — that is the whole fix, and it costs one command.** New first-hand: `PROJECT_SPEC.md` is **absent** from `paper-trader`, and its own `CLAUDE.md` names that file as the authority on intent; `docs/development_log.md` is still absent too. Nothing was done about any of it from here. The false "no dedicated backlog/TODO file" claim about this repo is **back** in this copy and was not acted on. **This close-out lands on a `claude/*` branch** — the branch requirement names `claude/nice-gates-kbqee5` — so run 200 will fire on `workflow_run`, documented routing and **not a regression**. **Per-session history belongs in `CHANGELOG.md`. Do not add a bullet here for a session that only verified.**
- **Do not manufacture a claim — and session 144 is the distinction, not the exception.** Standing since session 56: the routine's "claim exactly one backlog item" step is **vacuous when `Ready` is empty**, and inventing work to satisfy it is the failure this file has warned 30 sessions against. Sessions 41-143 correctly found true-zero and were right to. **Session 144 broke that run the only legitimate way — a real failure occurred and was observed — and session 145 closed the row it filed, so `Ready` is back to 0 and has been for sessions 145-149.** Filing a row for a defect you diagnosed first-hand is not manufacturing a claim; opening the backlog looking for something to promote is. **The test is whether the evidence existed before the row did.** Every remaining row is Done, Needs Human Review, launch-gated (BL-038), or gated on **D-009 alone** — the single decision between this repo and startable work, explicitly deferred by the owner rather than left unread. **The owner escalation is closed**: sessions 54 and 56 sent it; no later session should send a third. A question about another repository is never a reason to write to this one. **Before concluding there is nothing to do, run the two checks under Next Session** — a green deploy cannot see an unmerged branch or an unread issue, and sessions 71-99 each read this file, found zero `Ready`, and correctly concluded there was nothing to claim while ten days of owner-supplied answers sat behind a conflicted auto-merge.
- **The standing finding (session 100, still outstanding, not re-derived since)**: `main` has **never received the practice owner's answers**. `claude/nice-gates-sxxo8l` carries **D-014** (provider names, phone, flat $200 pricing, CA licence numbers removed site-wide) and **D-015** (the practice name), both recorded 2026-08-16 as approved in a conversational session; `src/lib/practice.ts` on `main` still reads `NEEDS_HUMAN_*`. Issues **#3/#4** are the auto-merge saying it could not land it. **Recorded, not acted on** — Absolute Rule 2 forbids editing provider credentials and pricing without a verified Tier 3 approval, and the routine's own constraint says this demo carries no real clinical content. Escalated once (session 100) with everything needed to act; **no second escalation has been sent and none should be.** Full detail in the session-100 `CHANGELOG.md` entry; port instructions under Next Session.
- **The lineage question is settled — do not re-derive it, and do not re-open it.** Sessions 100-102 recorded a "severed lineage / different root commits" finding; session 103 retracted it as a **shallow-clone artefact** and 104 confirmed the retraction on a complete clone. Settled: one true root `e591e8c` shared by every ref; merge base with `nice-gates-sxxo8l` is **`9609ec4`**, that branch **7 ahead**; `kind-newton-1mzu11` **fully contained in `main`** and not a finding; `git merge-tree` conflicts on exactly **two** files, `CHANGELOG.md` and `PROJECT_STATUS.md` — the two every session appends to — while every code and content file including `practice.ts` auto-merges cleanly. Derivation and disproof are in the session-103 entry. This changed the shape of the work, not the decision. **Run `git fetch --unshallow` before any `merge-base`, `rev-list`, root-commit or branch-count measurement, every time.** The harness clones shallowly **every** session. Three consecutive sessions built a false finding on a shallow answer, and session 117 measured first and got `kind-newton` **14 ahead** and `nice-gates` **249 ahead** — wrong, and entirely plausible-looking — before unshallowing and getting the real 0 and 7. **A shallow clone does not error; it answers, and the answer is wrong.** Sessions 104, 105, 113, 115, 117 and 118 each ran it and each got the settled numbers. Stop re-measuring it.
- **`git push` works here — repository-specific, settled, do not re-test it from this repo.** A push 403 means "no write access to *that* repo" and nothing about this one; `paper-trader`'s grant is read-only, re-confirmed from four distinct API surfaces, and needs no further testing from anywhere. **This has been re-derived ten times in four disguises** — as a control, as budget triage, as a control on another repo's diagnosis, and as "is the block session-wide?" — most recently by session 120, which also called the control *uninformative* on the grounds that with nothing to push, `--dry-run` short-circuits to `Everything up-to-date` before authentication — **session 125 showed that is wrong**: the short-circuit happens *after* a `git-receive-pack` ref discovery that itself requires write permission, so on a write-denied repo the dry-run returns the 403 and **does** answer the question. What stays forbidden is running it *from here about another repo*; run it **inside** the repo whose write path is unproven. Each cost nothing and each happened *before* the session reached this bullet, which is the structural point: **a session cannot read this warning until after the moment it would have prevented.** Another warning line is not the fix and none should be added. **The fix is on the other side, and it is the part still not routine: probe a repo's write path FIRST, before doing its work.** Sessions 93, 97, 112, 115 and 120 each did the work first and stranded 5, 7, 7, 12 and 11 green commits behind the 403; sessions 95, 104, 105, 116 and **121** probed first and stranded nothing beyond the one-commit probe itself. Copy those. **Session 121 is the first to do it without having read this bullet first — the practice had become habit — and it also established, from a push inside `paper-trader` to `paper-trader`'s own branch, that the block is repository-level rather than branch-level, so no target branch works around it. That is a same-repo test, not the forbidden cross-repo control, and it is now answered too: stop testing that as well.**
- **Deploy and CI are healthy, and five things about them are closed.** Deploy was `success` for **seventy consecutive runs (114-183)**; run **184** went red and **is now green on attempt 2**; **BL-045 (session 145) closed the gap it exposed** — `smoke` carries `timeout-minutes: 5` and the live-but-unverified distinction is in the job comment and DEPLOYMENT_AND_OPERATIONS_PLAN.md §Pipeline. **Two distinct failures now exist and they are nothing alike**: run 113 was a missing `concurrency` block (a repo defect, fixed); run 184 was **a job that never got a runner** (GitHub-side, fixed by re-running the failed job). **Before diagnosing any future red: check whether the job produced steps and a `runner_id`. If it did not, it never started — re-run it, and do not go looking in `astro.config.mjs`.** **There is exactly one deploy per landing; only the trigger differs** — a direct push to `main` deploys via `push`, a `claude/*` landing via the `workflow_run` chain off *Auto-merge claude branches* (and not also via `push`, because GitHub does not trigger `push` workflows from a `GITHUB_TOKEN` push). **Deploy-run count and trigger path are routing artefacts, never health signals**; check a run's `event` before treating a difference as a regression. `ci.yml` carries `workflow_dispatch` (BUG-007) — use it instead of pushing an empty commit when a run hangs. **Closed, do not re-open**: the `deploy-pages` step duration (5s vs 85s vs 1s-on-failure — slow and failing look nothing alike); BUG-008's serialisation, which needs two overlapping runs and **must not be manufactured**; push-trigger lag (session 39's 30-minute case was transient, 3s is typical); and **cross-repo credential tests must never be run against this repository**.
- **The routine's standing "FIRST PRIORITY: `withastro/action@v3` exit-code-1" instruction is stale**, and the scheduled prompt is what needs editing — **no session can fix it from inside this repository**. Eighty-nine sessions (32-143) failed to reproduce it, and **session 144 finally got a red deploy to examine — which refuted the clause instead of confirming it.** On run 184 `withastro/action@v3` was **green in 23s**; the red came from a *different* job, `smoke`, and from GitHub never scheduling it. So the clause names the wrong step. Both hypotheses were again refuted on that same red run (`--frozen-lockfile` clean in 9.3s, build 21 pages). **A red deploy here has meant a GitHub 5xx or a scheduling failure every single time, now including the one red run anybody has actually been able to inspect — read the job's `steps`/`runner_id` before suspecting `astro.config.mjs` or `pnpm-lock.yaml`.** Escalated in sessions 54 and 56; the channel is closed. The same prompt also names this repo's state file as "CHANGELOG.md at repo root", which does not exist — it is `docs/06_PROJECT/CHANGELOG.md`. **The prompt text is not stable between sessions, so report what your own copy says in one clause and infer nothing in either direction** about whether it is being edited.
- **`git fetch origin main` by itself.** `git fetch origin main claude/<branch>` **aborts entirely** — the session's `claude/*` branch is deleted from the remote after auto-merge and git fails the whole fetch on the bad refspec, leaving `origin/main` at the stale clone value. It reads exactly like six sessions of unpushed commits. Do not conclude work is unpushed from an aborted multi-refspec fetch.
- **Build status** — re-measured every session from a clean `node_modules`, unchanged since BL-040 on every count: `pnpm install --frozen-lockfile` clean; `pnpm build` **21 pages**; `pnpm lint`, `pnpm format` and `pnpm typecheck` (**0 errors / 0 warnings / 35 hints**) clean; `pnpm test` **160/160** across 24 files; `pnpm test:e2e` **587 passed / 3 skipped** (last measured session 115). The typecheck hint count is **35** — sessions 40-108 recorded 34 — and has been 35 for eleven sessions; it is a one-hint drift with no error attached and **should stop being re-flagged as new**.
- **Not independently confirmed** (do not restate as first-hand): the live site, since the sandbox egress proxy blocks `avrybrdly93.github.io` (curl exit 000), so the `smoke` job's curls from a hosted runner are the only evidence it serves; the Google Rich Results Test against deployed BL-031 data; and whether `github.io` is **HSTS-preloaded**, which session 97 established is unanswerable from here — `curl` gets a CONNECT tunnel 403 and the harness fetch tool `EGRESS_BLOCKED`, so it is proxy policy, not a `curl` artefact. **Do not answer it from model memory** — an unverified belief written up as a finding is the fabricated completion claim D-012's own "Alternatives considered" rejected. It stays `unknown` in R-013 and BL-038.

## Current Focus

M3: BL-022 (`/api/contact` backend) gated on D-009 — **deferred by the owner on 2026-08-25** ("no hosting platform or email vendor yet, let's bypass that for now and come back to it"), so do not re-attempt or re-raise it until they return to it. M4: BL-032 still Needs Human Review; BL-033 **Done** and D-012 **resolved** (2026-08-25); BL-038 carries the deferred header + monitor work as a launch blocker. The owner's stated near-term goal is a **strong prototype to demo to the practice owners before go-live** — weigh any future work against that, not against launch-readiness.

## Ready

**None — and that is the correct end state, not a gap to fill.** BL-045 was the only `Ready` row and session 145 closed it. Every remaining row is Done, Needs Human Review, Blocked on deps, launch-gated (BL-038), or gated on **D-009** alone. **Do not promote any of those to fill the queue, and do not manufacture a claim** — session 144 is the distinction and not the exception: it filed BL-045 because a real failure occurred and was diagnosed first-hand, so the evidence preceded the row. Opening the backlog looking for something to promote is the failure this file has warned against for 30 sessions. **The test is whether the evidence existed before the row did.**

## In Progress

| Item | Next step |
| --- | --- |
| BL-022 | D-009 (Tier 3, Proposed) needs a human to name a hosting platform + email vendor. Once resolved: stand up the function against `ContactForm.client.ts`'s existing `fetch('/api/contact', {method:'POST', …})` call (no client rework expected), add server-side rate limiting, verify real delivery, flip to Done. Page, form UI, validation, honeypot, success/failure states and submit-outcome analytics are all shipped and tested. |

## Blocked / Needs Human Input

| Item | What's needed |
| --- | --- |
| **D-009** | Hosting platform + email vendor for `/api/contact`. **The only decision still blocking startable work**, and now more leveraged than before: it also supplies BL-038's header-delivery mechanism if the chosen host supports `_headers`. **Owner deferred it 2026-08-25** — awaiting their return to it, not their attention. |
| **BL-042** | *(not urgent — owner-side, not blocking anything today; **do not escalate**.)* Confirmation that `sitemap.xml` was submitted in Search Console, which D-016 makes the only working discovery path on the current project-site deployment. Unverifiable from any agent session — it is an action in a Google account, and the egress proxy blocks the live site. Closes equally if the site moves to its own origin (D-016's rollback path). |
| **BL-038** | *(not urgent — launch-gated, not blocking today.)* Header-delivery mechanism and uptime-monitor vendor, both deferred by D-012 on 2026-08-25. Needed before go-live, not before the owner demo. |
| Practice constants | **Answered on 2026-08-16 and never landed — see the Last session bullet.** `claude/nice-gates-sxxo8l` carries D-014/D-015 with names, phone, $200 flat pricing, licence-number removal and the practice name. `main` still shows `NEEDS_HUMAN_*`. **What is needed from the owner is now a confirmation that those decisions are theirs and a go-ahead to port them**, not the values themselves. Still genuinely unfilled either way: `PLACEHOLDER_EMAIL`, `PLACEHOLDER_DOMAIN`, cancellation policy, accepted payment methods. |
| Provider bios | Approach statements (first-person, ≤150 words), education/training lists, bio body copy (fills `src/content/providers/*.md` NEEDS_HUMAN placeholders) — Tier 3. |
| Legal copy | Counsel-approved Privacy Policy, Terms, telehealth consent, Accessibility Statement — Tier 3; `src/content/legal/*.md` are placeholder shells (`reviewStatus: needs-human-review`). |
| FAQ content | `/faq`'s 13 Q&As are AI-drafted and need clinical/practice review before publish; cancellation-policy and payment-methods answers await the practice-constants row. |
| Vendor selection | Scheduling/intake/video vendor chosen + BAA signed (R-004) + booking URL format. |
| Provider photos | Professional photos per IMAGE_GUIDELINES.md. |
| **BL-044** | *(not urgent — documentation hygiene, blocks nothing.)* This file's own 60-line cap is unreachable with BL-043's safeguards intact — session 119 reached **70** and the arithmetic in BL-044 shows the floor. A human picks one: raise the cap, move the standing warnings to a page every session must read, or accept fewer warnings. **Do not resolve it by trimming further.** |
| Human review | BL-012, BL-015, BL-032. |

## Next Session

**Expect zero `Ready` rows and confirm it by tallying `BACKLOG.md`'s own status column, not by trusting this file.** Then run the two checks below, confirm the gate, close out. **Do not manufacture a claim.**

1. **List the repository's open issues.** Two, #3 and #4, both bot-filed 2026-08-16, both pointing at the stranded branch. A third number would be genuinely new — read it rather than assuming it is #3 or #4 again.
2. **List `claude/*` branches `main` does not contain — with `git ls-remote --heads origin`, after `git fetch --unshallow`**, or the answer is wrong twice over. On complete history there is **one**: `nice-gates-sxxo8l`, 7 commits. `kind-newton-1mzu11` is contained in `main` and must not be reported. **Do not use `git branch -r`**: it reads the local clone's refs, and sessions 104 and 105 each found it listing a phantom harness branch `ls-remote` showed did not exist. Your own harness branch appears under a new name every session, so check whether a name is *ahead* before treating it as a finding; a branch at 0 ahead never is.

**Step one is one call:** re-read issues **#3** and **#4**. If `updated_at` still equals `created_at`, the owner has not answered — leave it and **do not re-escalate**.

- **If the owner has confirmed the decisions and said to land them**: that is a task list, so the "manufacture no claim" rule does not apply. Port the three content commits (`8a5d6e5`, `db89697`, `2993116` — 13 files, ~34 insertions / 45 deletions) onto `main` as one change, re-run the full gate, update `practice.ts`, adopt D-014/D-015 into `DECISION_LOG.md` here, flip BL-012's names/price/licence sub-items, and close issues #3 and #4 naming the commit that superseded them. **Merge it.** Run `git fetch --unshallow` first, then `git merge-tree --write-tree` to confirm the conflict set has not grown past `CHANGELOG.md` and `PROJECT_STATUS.md`; resolve those two by hand, keeping `main`'s sessions 70-onward entries and interleaving the branch's session 70/71 entries in date order. Every code and content file auto-merges.

**Also worth one look, and no more:** the e2e suite is 590 cases (587 run, 3 skipped), ~1.8 minutes locally. If CI's `e2e-axe-lighthouse` job time becomes a problem, the answer is **sharding, not deleting coverage**. And **do not create or push an empty `claude/*` branch.**
