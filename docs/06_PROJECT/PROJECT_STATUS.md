---
title: Project Status
status: Active
authority: Project
owner: Engineering
dependencies:
  - BACKLOG.md
  - CHANGELOG.md
review_cycle: Every session
---

# Project Status

> Updated by the agent at the END of every session (EXECUTION_LOOP.md Phase 5). This file is the first thing every session reads. Keep it under 60 lines: current truth only — history lives in CHANGELOG.md.

## Snapshot

- **Phase**: M1/M2 done. M3 — `/book` Steps 1-4 Done (BL-035/036/037/021); BL-022 (`/api/contact` backend) is the only M3 gap, gated on D-009. M4 — BL-030/031/018 Done; BL-032 Needs Human Review; BL-033 gated on D-012. BUG-001…008 Done.
- **Last session**: 2026-08-23 (session 89). **Verification-only; nothing claimable, nothing shipped.** **Deploy run 125 at `1ffd51c` — `main`'s HEAD, and session 88's own closing commit — is green on all three jobs**: `build` 27s (`withastro/action@v3` success in 21s), `deploy` 9s (`actions/deploy-pages@v4` **6s**), `smoke` 5s with all three checks passing, **`push` trigger exactly as session 88 predicted** (it pushed its close-out straight to `main`), attempt 1, 52s end to end — the second consecutive session to call its own routing in advance. **Runs 114-125 are twelve consecutive successes.** The `deploy-pages` step is **recorded, not investigated**: the range across those twelve is 5s / 85s / 6s / 6s / 63s / 6s / 6s / 6s / 6s / 6s / 6s / 6s while run 113's *failure* took 1s — the eighth in a row at 6s on a question closed three times. Do not reopen it. Still only one deployment in flight, so BUG-008's serialisation remains **unobserved**, unchanged since session 78. Local gate clean at `1ffd51c`: install `--frozen-lockfile` **8.9s**; lint clean; typecheck **0 errors / 0 warnings / 34 hints**; test **160/160 across 24 files**; `pnpm format` clean; readability **16/0/2**; build **21 pages in 2.74s** — every figure matching sessions 79-88 except install and build wall-clock. `BACKLOG.md` unchanged: **zero `Ready`** across 36 item rows (29 `Done`, 3 `Needs Human Review`, 2 `In Progress`, 1 `Blocked`); D-009 (line 248) and D-012 (line 434) still the only two `Proposed`.
- **`git push` works here** — repository-specific, and confirmed seven times from two directions. Session 77 hit a **403 on `GET /info/refs?service=git-receive-pack`** pushing to *`paper-trader`* and briefly wrote it up as environmental before testing here. Session 79 hit the identical 403 on that same repository and, in the same session and with the same GitHub App, **created a ref on `avrybrdly93/launcher` successfully** while `paper-trader` returned `403 Resource not accessible by integration`. Sessions 81, 82, 84, 85, 86 and **88** each made the same `git push --dry-run` check here — nil cost (no ref, no run, no deployment) but unnecessary, since this bullet already answers it. A push 403 means "no write access to *that* repo" and nothing about this one. **The question is answered; do not re-test it here.** **Six sessions re-derived it anyway, in four different disguises** — 82 as a control it then mis-described, 84 as *budget triage* ("is the block session-wide?"), 85 and 86 as a *control on `paper-trader`'s diagnosis*, and 88 as *diagnostic scope* ("is the 403 this repo or the whole session?"). **This bullet answers all four framings.** Read it before probing, not after — another warning line here is not the fix, and no session should add one. **Session 89 is the first clean case that counts: it hit the 403 first** (it orders the repositories oldest-log-first and `paper-trader` has no dated log entry at all, so that repo went first), **arrived here holding it, read this bullet, and ran no control.** That is restraint, not the session-87 ordering accident — 87 reached `paper-trader` last and so had no 403 in hand. The streak of re-derivations stands at six and is not currently running.
- **Standing, carried forward and re-confirmed each session since 56**: the scheduled routine's step 3 ("claim exactly one backlog item before writing code") is **vacuous with no `Ready` row**, and manufacturing a claim to satisfy it is the invented work this file has told 21 sessions not to do. **Autonomous sessions have no runway left here** — every remaining item is Done, Needs Human Review, Blocked on deps, or gated on D-009/D-012, so the next unit of progress is a human answering one of them. **The owner escalation is closed**: sessions 54 and 56 sent it on separate dates, sessions 57-87 sent no third, and no later session should. A question about *another* repository is never a reason to write to this one — session 74 held that line against the `paper-trader` 403 and sessions 75-76 did the same.
- **Build status** -- all measured locally in session 89 at `1ffd51c`, fresh `pnpm install --frozen-lockfile` (Node 22.22.2, pnpm 10.33.0, clean in 8.9s): typecheck **0 errors** (0 warnings, 34 hints); lint, format clean; `pnpm test` **160/160 across 24 files**; `check:readability` **16 passed / 0 failed / 2 skipped**; `pnpm build` **21 pages** in 2.74s. The test count moved once, in session 78 -- 156/23 to 160/24, BUG-008's four assertions in `tests/unit/deploy-workflow.test.ts` -- and has been stable since; every other figure is identical to sessions 40-88 except install and build wall-clock, which are container-speed artefacts and not signal. **Playwright and `lhci` figures are session 85's and remain current: `pnpm test:e2e` 274 passed / 2 correctly skipped; `lhci autorun` 21 URLs / 21 runs, zero assertion failures.** Sessions 86-89 did not re-run them. **`lhci` needs `CHROME_PATH` here.** Bare `pnpm exec lhci autorun` dies at its own healthcheck (`❌ Chrome installation not found`) *and exits 0 doing so*, so it looks like a pass to anything checking only the exit code. Use `CHROME_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome pnpm exec lhci autorun`, and read its output, not its exit code. Note also: the format gate is `pnpm format` (it runs `prettier --check`); there is no `format:check` script.
- **Fetch `main` by itself.** `git fetch origin main claude/<branch>` **aborts entirely** — the session's `claude/*` branch is deleted from the remote after auto-merge, and git fails the whole fetch on the bad refspec, leaving `origin/main` at the stale clone value. Session 64 hit this and it reads exactly like six sessions of unpushed commits; `git fetch origin main` alone showed `origin/main..HEAD` empty. Do not conclude work is unpushed from an aborted multi-refspec fetch.
- **CI: green on both trigger paths at `f90832e`** (session 40, unchanged since — no code has landed). The `workflow_dispatch` run `31154026561` and the push run `31154019192` at that SHA each passed **both** jobs independently — `lint-typecheck-build` 58s, `e2e-axe-lighthouse` 5m45s. Every job got a runner immediately, so the session-38/39 scheduling failure is not recurring. **`ci.yml` can be re-triggered by hand** (BUG-007): `workflow_dispatch:` sits alongside the untouched `push`/`pull_request`/`workflow_run` triggers and returns HTTP 204 against `main` — use it instead of pushing an empty commit when a run hangs.
- **Push-trigger lag is not a standing condition.** `f90832e` pushed at 06:27:47Z, its `ci.yml` run created 06:27:50Z — 3 seconds. Session 39's genuine 30-minute lag at `9ba82ee` was transient. Do not budget 30 minutes for it, and do not conclude from a short check that a push failed to trigger. A BUG-004 regression remains ruled out.
- **Deploy: green at `main`'s HEAD.** Run **125** at **`1ffd51c`** concluded `success` with all three jobs green — `build` 27s (`withastro/action@v3` 21s), `deploy` 9s (`actions/deploy-pages@v4` **6s**), `smoke` 5s with all three checks passing. Runs 114-124 were the same: **twelve consecutive successes, 114-125.** **The `deploy-pages` step duration is not a symptom in either direction** — 5s, 85s, 6s, 6s, 63s, 6s, 6s, 6s, 6s, 6s, 6s and 6s across runs 114-125, all successes; run 113's *failure* took **1s**, so slow and failing look nothing alike here. It is **closed, and re-closed three times over**. Do not investigate it. Run **113** at `49f1d9c` remains the one `failure` in runs 102-125, caused by the missing `concurrency` block. **What runs 114-125 do not prove:** each was the only deployment in flight, so they show the workflow still deploys, not that the serialisation queues a second run. That needs two overlapping runs; take the observation if it arises naturally and do not manufacture it. Session 71 caused run 104 as a cross-repo credential control and should not have; sessions 72-89 did not repeat it. **Do not run cross-repo credential tests against this repository.** **Deploy-run count and trigger path are routing artefacts, not health signals.** There is **exactly one deploy per landing; only the trigger differs.** A direct push to `main` deploys via `push`. A `claude/*` landing deploys via the `workflow_run` chain off **`Auto-merge claude branches`** — and *not* also via `push`, because that workflow merges using `actions/checkout@v4`'s default `GITHUB_TOKEN`, and GitHub does not trigger `push` workflows from a `GITHUB_TOKEN` push. Run 125 is the `push` path, and session 88 predicted it in advance from pushing its close-out straight to `main`, as session 87 predicted run 124's `workflow_run` from its harness branch pin — the model has now called the route before the fact twice running. A SHA showing **both** (`eec9cb7`, `3c27d08`) reached `main` by both routes in one session. Sessions 50 and 51 misread this as a trigger regression; session 52's first attempt at the mechanism was also wrong and is corrected in its CHANGELOG entry. Session 58's extra run at `3b3527b` came from its own empty second landing, not a second route — do not investigate it. Session 66's `push`-where-`workflow_run`-was-predicted caveat was closed in session 68 and the `CI_MERGE_PAT` check is **not** needed; do not reopen it. Session 38's outage is long cleared; two of its conclusions were wrong and are corrected in the session-39 CHANGELOG entry.
- **The routine's standing "FIRST PRIORITY: `withastro/action@v3` exit-code-1" instruction is stale** and should be edited out of the scheduled prompt. **Fifty-six sessions (32-89)** have confirmed it does not reproduce, and `--frozen-lockfile` has installed cleanly every time — 8.9s in session 89 — refuting the prompt's lockfile-mismatch hypothesis too. Session 54 escalated this to the owner by push notification and session 56 sent it a second and final time; the escalation channel is now closed; sessions 57-89 each sent no third. Session 89, like 64, 67 and 70-88, tested both of the prompt's hypotheses directly rather than inheriting the conclusion: the lockfile one is refuted by an 8.9-second clean `--frozen-lockfile` install, the `astro.config.mjs` one by `pnpm build` completing 21 pages and by run 125's own `withastro/action@v3` succeeding in 21s, neither of which a config syntax error permits. A red `withastro/action@v3` has meant a GitHub 5xx or a scheduling failure every time — **read the job log before suspecting `astro.config.mjs` or `pnpm-lock.yaml`.**
- **Not independently confirmed** (unchanged, do not restate as first-hand): the live site — the sandbox egress proxy blocks `avrybrdly93.github.io` (re-tested session 39, curl exit 000), so the `smoke` job's curls from a hosted runner are the only evidence it serves; and the Google Rich Results Test against deployed BL-031 data, which needs a tool this environment lacks.

## Current Focus

M3: BL-022 (`/api/contact` backend) gated on D-009 — do not re-attempt until DECISION_LOG.md shows it resolved. M4: BL-033 gated on D-012; only the header-delivery mechanism and monitor vendor remain, its smoke-test sub-item shipped in session 32. Everything else in M3/M4 is Done or Needs Human Review.

## Ready

_(none — unchanged since BUG-007 shipped in session 40, re-confirmed in sessions 41, 42 and 43. The project is human-gated: see Blocked / Needs Human Input.)_

## In Progress

| Item | Next step |
| --- | --- |
| BL-022 | D-009 (Tier 3, Proposed) needs a human to name a hosting platform + email vendor. Once resolved: stand up the function against `ContactForm.client.ts`'s existing `fetch('/api/contact', {method:'POST', …})` call (no client rework expected), add server-side rate limiting, verify real delivery, flip to Done. Page, form UI, validation, honeypot, success/failure states and submit-outcome analytics are all shipped and tested. |
| BL-033 | D-012 (Tier 3, Proposed) needs a human to pick a header-delivery mechanism (CDN/proxy in front of Pages, a hosting migration that would also resolve D-009, or accept the gap as documented residual risk) and an uptime-monitor vendor. Once resolved: deliver the missing `X-Content-Type-Options`/`X-Frame-Options`/`Permissions-Policy`/HSTS headers, stand up monitoring on `/` and `/book`, flip to Done. CSP + Referrer-Policy meta tags, e2e coverage and the smoke job are shipped. The deferred contact-function healthcheck needs BL-022 first. |

## Blocked / Needs Human Input

| Item | What's needed |
| --- | --- |
| **D-009** | Hosting platform + email vendor for `/api/contact`. Highest-leverage decision here — per D-012 a hosting migration would unblock both items at once. |
| **D-012** | Header-delivery mechanism + uptime-monitor vendor. |
| Practice constants | Real provider names, credentials, CA license numbers, prices, phone, email, domain, cancellation policy, accepted payment methods (fills `practice.ts` placeholders). |
| Provider bios | Approach statements (first-person, ≤150 words), education/training lists, bio body copy (fills `src/content/providers/*.md` NEEDS_HUMAN placeholders) — Tier 3. |
| Legal copy | Counsel-approved Privacy Policy, Terms, telehealth consent, Accessibility Statement — Tier 3; `src/content/legal/*.md` are placeholder shells (`reviewStatus: needs-human-review`). |
| FAQ content | `/faq`'s 13 Q&As are AI-drafted and need clinical/practice review before publish; cancellation-policy and payment-methods answers await the practice-constants row. |
| Vendor selection | Scheduling/intake/video vendor chosen + BAA signed (R-004) + booking URL format. |
| Provider photos | Professional photos per IMAGE_GUIDELINES.md. |
| Human review | BL-012, BL-015, BL-032. |

## Next Session

**Session 90 has no inherited task: run 125 at `1ffd51c` is green on all three jobs and nothing is claimable.** **Expect session 89's close-out to have landed via `push`** — its harness pinned it to `claude/festive-meitner-2rvpan` but this repository's own CLAUDE-level convention and the routine both point at `main`, and sessions 84-86 and 88 pushed there directly. Either route is normal; **check the run's `event` before treating a difference as a regression**, and see the deploy bullet above. Keep the sessions 53-89 pattern: gate re-run, no restatement of the audit, a short entry recording only what is new. **Do the deploy check first, before concluding there is nothing to do** — that is what found BUG-008 after twenty-five sessions that were right about the backlog and wrong about the repository. Each session's own close-out commit becomes `main`'s HEAD *after* that session ends, so the run at HEAD is always the one the previous session could not see; checking it is not redundant. Then check D-009 (DECISION_LOG line 248) and D-012 (line 434); if both are still `Proposed` and `BACKLOG.md` still has no `Ready` row, run the local gate (`pnpm install --frozen-lockfile`, lint, typecheck, test, `pnpm format`, `check:readability`, build) and write at most three lines. **When counting `BACKLOG.md`, key on row contents, not `awk -F'|'` field position** — session 86 found that `BUG-005`'s description carries an inline `|` inside backticks, shifting that one row's fields by one and reporting its status as `—`. It is `Done`; the zero-`Ready` conclusion is unaffected. If a run is red, **read the job log first**; the scheduled prompt's `astro.config` / lockfile hypotheses have now failed to reproduce for 56 sessions. **Do not create or push an empty `claude/*` branch** — sessions 58, 71 and 77 each did, and each cost a no-op landing plus a redundant deploy run; create the branch only once there is a commit to put on it. Do not re-derive the human-gated audit (session 33's CHANGELOG entry holds it) and do not invent a task to fill the session. **Three things are closed for good and must not be re-opened**: the `paper-trader` 403 must not be re-tested from here — answered eight times (77, 79, 81, 82, 84, 85, 86, 88), with **session 88 the cautionary example** (it hit the 403 first and probed here before reading the bullet) and **session 89 the one to copy** (it also hit the 403 first, read the bullet, and ran nothing), so **read the `git push` bullet before you probe, not after**; the `deploy-pages` step duration must not be investigated (twelve successes span 5s-85s and the failure mode is 1s); and BUG-008's serialisation must not be manufactured.

**The owner escalation is closed — send none.** Sessions 54 and 56 sent it on separate dates, sessions 57-89 correctly sent no third, and neither should any session after. (Sessions 79, 81, 82, 84, 85, 86, 88 and 89 *did* each send the owner a notification, about `paper-trader` being unpushable — a different repository and a live blocker with work stranded behind it. Session 89's stranded six commits there, including the finished backlog item `F6`, and carries the diagnosis and the fix as session 84's did: the 403 is a repo missing from the Claude GitHub App's grant that already covers this repo and `launcher`. That is not this repo's escalation and does not reopen it.) The two asks stay on the record — edit the stale `withastro/action@v3` FIRST PRIORITY out of the scheduled prompt, and pause this leg until a human answers D-009 — but the lever is the owner's, not this session's. Recording the condition in the changelog line is the whole obligation.

**What unblocks this repo is one human decision, and D-009 is the one to make.** Per D-012's own analysis a hosting migration off GitHub Pages resolves *both* gates at once — BL-022's `/api/contact` function gets a runtime, and BL-033's security headers get a delivery mechanism. Answering D-009 alone therefore converts two `In Progress` items into startable work, and the `contact function healthcheck` smoke TODO becomes implementable the moment BL-022 ships — claim it that same session.

`ci.yml` can be **dispatched by hand** (BUG-007) — but only when something has actually moved or a push-triggered run hangs; sessions 41-86 all skipped it because deploy was already green at HEAD.

## Weekly Review Findings

_(most recent only; older → CHANGELOG.md)_

- **2026-08-15 (session 67)**: This file remains **over its own ≤60-line rule**; session 67 rewrote lines rather than adding them. The overage is still the Blocked / Needs Human Input table plus the two In Progress rows — live human-gate state, not history — and **it shrinks when a human resolves D-009 or D-012, not before.** **Standing observation, now twenty-eight sessions old (40-67): no product change, because every remaining item is human-gated, and the scheduled prompt's "FIRST PRIORITY: `withastro/action@v3` exit-code-1 / check `astro.config` / check the lockfile" has failed to reproduce across thirty-six sessions (32-67) with thirty-six clean `--frozen-lockfile` installs.** Session 67 re-tested both hypotheses directly rather than inheriting the conclusion, and again checked the last **30** `deploy.yml` runs: **zero non-success**. **The one new fact is a closure, not an opening:** session 66's unexplained `push` event is resolved — at `4021082` the event is `workflow_run` as the session-52 model predicts, so that was a one-off at a directly-pushed SHA and the `CI_MERGE_PAT` check session 66 proposed is unnecessary. Escalation stays closed; sessions 54 and 56 exhausted that channel and 57-67 correctly sent nothing. The remaining lever is the owner's.
